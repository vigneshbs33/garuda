# GARUDA — Python Requirements
# Requires Python 3.10+ (open-image-models, used for license plate detection, needs it)
# Install: pip install -r requirements.txt
# GPU:     pip install -r requirements.txt torch --index-url https://download.pytorch.org/whl/cu121

# ===== Core ML =====
ultralytics>=8.0.0           # YOLO11n detection + tracking
opencv-python-headless>=4.8.0 # Image processing
numpy>=1.24.0                # Array operations
Pillow>=10.0.0               # Image I/O

# ===== Preprocessing =====
albumentations>=1.3.0        # Training augmentation

# ===== License plate OCR =====
# Plate detection (locates the plate inside the vehicle crop — requires Python 3.10+)
open-image-models[onnx]>=0.5.0

# Plate text reading
easyocr>=1.7.0

# Optional / inert fallbacks (not reliably functional in this project's environment —
# kept only because ml/pipeline/ocr.py will use them automatically if available):
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
# pytesseract>=0.3.10

# ===== Driver State =====
mediapipe>=0.10.0            # FaceMesh for drowsiness/yawn detection

# ===== Federated Learning =====
flwr[simulation]>=1.5.0      # Flower FL framework

# ===== Deep Learning =====
torch>=2.1.0                 # PyTorch (CPU) — GPU via pytorch.org
torchvision>=0.16.0

# ===== Backend =====
fastapi>=0.104.0             # Async web framework
uvicorn[standard]>=0.24.0    # ASGI server
pydantic>=2.5.0              # Data validation
pydantic-settings>=2.1.0     # Settings from env file
sqlalchemy>=2.0.0            # Async ORM
aiosqlite>=0.19.0            # SQLite async driver (dev)
asyncpg>=0.29.0              # PostgreSQL async driver (prod)
python-multipart>=0.0.6      # File upload support
imageio-ffmpeg>=0.6.0        # Bundled static ffmpeg binary — re-encodes rendered
                              # videos to browser-playable H.264 (OpenCV's own
                              # mp4v/avc1 VideoWriter output isn't browser-decodable)

# ===== Alerts =====
twilio>=8.0.0                # WhatsApp/SMS alerts (optional)

# ===== Dev tools =====
python-dotenv>=1.0.0         # .env file loading
httpx>=0.25.0                # Async HTTP client (testing)
pytest>=7.4.0                # Testing
pytest-asyncio>=0.21.0       # Async test support
